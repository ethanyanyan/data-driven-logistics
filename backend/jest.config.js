/**
 * Options used to configure Jest, the testing
 * library we'll be using for our backend code.
 */

module.exports = {
  testEnvironment: "node",
  verbose: true,
  testPathIgnorePatterns: ["/node_modules/"],
};
