import { Outlet } from "react-router-dom";

export default function DataDrivenLogistics() {
    return <div>
        <div style={{ margin: "1rem" }}>
            <Outlet />
        </div>
    </div>
}